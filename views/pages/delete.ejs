<%- contentFor('body') %>
 <!-- <i class="material-icons">face</i> -->
 <div class= 'container' id = 'gerenciar'>
   <h2 id='titulo'>Gerenciamento de Clientes</h2>
   <div id='ajuda'>

    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
    ?
    </button>
  
  <!-- Modal -->
  <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Ajuda</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body" style = "text-align: justify;">
            <p aling ="justify">
                Ao clicar no botão <b>Excluir</b>, ao lado do nome do paciente, os dados do cadastrado serão removidos do sistema. <br>
            O botão <b>Editar</b> serve para alterar as informações do paciente no sistema, ao clicar no botão você será redirecionado para uma nova pagina onde
            será possivel ver todas as informações do paciente
            </p>
        </div>
      </div>
    </div>
  </div>
  
   </div>
        <div class="border" id='clientes'>
            <%if(Cliente.length== 0){%>
                <div class="alert alert-primary" role="alert">
                    <h4 class="alert-heading">Não há pacientes cadastrados</h4>
                    <hr>
                    <p class="mb-0">Comece a cadatrar os pacientes,clique no botão abaixo.</p>
                    <p></p>
                    <a href="/cadastro" class="btn btn-primary" type="submit">Cadastrar pacientes</a>
                </div>
            <%}else{%>
        
            <!-- loop fica aqui -->
            <%for(var i = 0; i < Cliente.length; i++){%>
            <div id = 'cliCard' class="card">
                <div id = 'cliente' class="card-body">
                    <div id = 'itemCliente'>
                        <i class="material-icons md-48">account_circle</i>
                    </div>
                    <h5 id = 'nome' class="text-left">
                        <%=Cliente[i].nome%>          
                    </h5>
                    <div id = 'deletar' style="margin-left:auto">
                        <a  id='deletarBotao' href="/cliente/<%=Cliente[i].cpf%>" class="btn btn-primary"  type="submit">editar</a>
                        <a id='deletarBotao' href="/delete/<%=Cliente[i].cpf%>" class="btn btn-primary" type="submit">excluir</a>
                    </div>
                </div>
              </div>
            <%}%>
        </div>
        <%var tamanho = Cliente.length/5%>
            <%if(tamanho < 0){%>
                <%tamanho = 1;%>
            <%}%>
            <nav id = 'page'aria-label="Navegação de página exemplo">
                <ul class="pagination">
                    <%if(Page == 0){%>
                        <li class="page-item disabled">
                            <span class="page-link">Anterior</span>
                        </li>
                    <%}else{%>
                        <li class="page-item"><a class="page-link"  href="/gerenciar/<%=(Page -1)%>">Anterior</a></li>
                    <%}%>
                    <%if(Cliente.length < 5 || Maximo.length == 5){%>
                        <li class="page-item disabled">
                            <span class="page-link">Próximo</span>
                        </li>
                    <%}else{%>
                        <li class="page-item"><a class="page-link" href="/gerenciar/<%=(Page +1)%>">Próximo</a></li>
                    <%}%>
                </ul>
              </nav>
        <%}%>
</div>
